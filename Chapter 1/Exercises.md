Option a; Store information for long term even beyond a power cycle.
A program is a set/sequence of instructions written for the CPU of a computer to carry out some task.
An interpreter reads a source code and interpretes the statements line by line as the programmer programs while the compiler handels the entire program files and also translates it to machine language.
Option c: Python Source file
There is a syntax error in the code, "print" was not properly spelt.
Main memory.
44.
The Central processing unit is the part of the computer that carries out instructions. It works like the human brain that processes the actions of the whole body. The main memory is also like the brain but basically the part brain that stores information that the body needs to carry out immediately. The secondary memory can be likened to past memory that the brain can fetch out later when needed, it can also include external storage material like notebooks, or notetaking applications. Input devices can be likened to the body sensory parts, eyes, ears, skin, mouth and nose. Output also includes the mouth, legs, hands etc.
A sytax error is fixed by a process called debugging.